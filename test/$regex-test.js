var _ = require('underscore'),
    expect = require("expect.js"),
    sift = require(".."),
    assert = require("assert");


describe("$regex#", function () {
    xit("can be evaluated with an object", function () {

    });
    xit("can be evaluated with a string", function () {

    });
});